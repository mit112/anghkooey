name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: macos-26

    steps:
      - uses: actions/checkout@v5

      - name: Install dependencies
        run: brew install ripgrep

      - name: Run CI
        run: bash scripts/ci.sh
