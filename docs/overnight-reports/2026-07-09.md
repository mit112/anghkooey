# Overnight backlog run — 2026-07-09 (session 2: Epic #10 + tooling #56)

## 1. One-line verdict
**Epic #10 (capture/AI, all 6 sub-issues) and tooling #56 are complete, both GPT-5.5 checkpoints passed with findings fixed, and everything is merged to `overnight/backlog-2026-07` with a full green suite.** Stretch tiers were not attempted (stopped on request at a clean checkpoint); two small observability follow-ups found by a checkpoint were filed as issues #85/#86 (not fixed).

## 2. ⚖️ Arbitrated decisions (GPT-5.5) — ratify or reverse before this branch reaches `main`
All reversible in code; **no on-disk schema or data-contract changes** were made this session.

- **#31 — Capture-tab segment layout = Option A `Type | Camera | Cloze`, defaulting to `Type`.** GPT-5.5-arbitrated (full memo logged as a `⚖️ DECISION` comment on issue #31). The toolbar-toggle alternative was rejected because it hides a primary capture path. Default `Type` chosen because it's the lowest-friction entry (works on the simulator and when on-device AI is unavailable — it falls back to a stub with the #30 banner explaining why), whereas Camera is unavailable on the simulator. Fully reversible: pure `CaptureMode` enum + segmented Picker, one-line change to reorder or re-default.
- **#30 — dropped the "deep-link to Settings" idea (secondary, reversible).** The availability banner's message already contains the "Turn on Apple Intelligence in Settings…" guidance *text*; a Settings-opening button isn't verifiable in an unattended run, so it was omitted. Reversible (add a `Button`/`openURL` later).

Session-1's `#28` decision (dropped-capture drop-with-message) still stands and still awaits ratification (see `2026-07-08.md` §2).

## 3. Issues completed / filed, per epic

### Epic #10 — Capture & AI authoring ✅ (all 6 merged)
| Issue | PR | Notes |
|---|---|---|
| #29 multi-draft enqueue | #76 | drains full `generateDrafts` stream; per-capture `batchID` → interleave-safe "Card N of M"; zero-yield/partial-error handling. silent-failure-hunter BLOCK → fixed (`String(describing:)` since `AuthoringError` isn't `LocalizedError`; log throw-before-draft). |
| #32 swipe-dismiss advances queue | #77 | `handleSheetDismiss()` advances iff `presentedDraft == nil` (state-based, version-independent — no fragile one-shot flag). + gated in-flight-persist race test. |
| #30 availability banner + fallback tag | #78 | wired the dead `CaptureAvailabilityModel`; `IdentifiedDraft.isFallback` → sheet "AI unavailable" note; refresh on scene-active. |
| #34 drafting-latency indicator | #79 | `authoringCount` (Int, `defer`-decrement); centralized top-inset slot (drafting > clipboard); VoiceOver announce only on 0↔n. |
| #31 typed-text capture | #80 | ⚖️ Layout A; new `TypedTextCaptureView` funnels through the same `enqueue`. |
| #33 clipboard changeCount dedupe | #81 | replaced the dead SHA hash-ring with `UIPasteboard.changeCount` dedupe + `PasteboardReading` seam; deleted `consider()`/`OfferHashStore`. |
| **Epic #10 checkpoint hardening** | #82 | GPT-5.5 found a `batchCounts` leak on swiping the last/only card (#32 × #29) → `pruneStaleBatchCounts()` full sweep + regression tests. |

### Tooling — #56 ✅ (merged)
| Issue | PR | Notes |
|---|---|---|
| #56 make generate auto-patch | #83 | `make generate` now self-heals: runs `patch_privacy_info.py` (Makefile + `postGenCommand`), keeps `NSCameraUsageDescription` + `LSSupportsOpeningDocumentsInPlace` (added to `project.yml` `info.properties`), and `ci.sh` fails if any target's PrivacyInfo is missing. Verified idempotent (identical shasums over two runs) and Info.plist byte-identical to committed (no key lost). |
| **Tooling checkpoint hardening** | #84 | GPT-5.5 found CI didn't guard the `project.yml → Info.plist` contract → `ci.sh` now fails if either camera key is dropped from `project.yml`. |

### Follow-ups filed (NOT fixed — small observability gaps from the Epic #9 checkpoint)
- **#85** — `OptimizedParametersStore.loadOptimized` swallows corrupt-file errors (should distinguish file-not-found from a corrupt/unreadable file and log).
- **#86** — `GradeCardIntent` parse-guard silently returns success on a bad cardID/rating (should log the dropped grade).
- `SettingsView` `try? freeze.unfreeze()` still folds into stretch issue #48 (not touched this session).

## 4. Suite status & checkpoint findings
- **Before (session-2 start):** full `ci.sh` green — app-target 144 tests / 32 suites (session-1 tip).
- **After (integration tip):** full `bash scripts/ci.sh` green — Core **150**, Intelligence **43**, UI **63** (+4 from #31's `isDraftable` tests), app-target **165** / 35 suites. Every merge was gated on a green full `ci.sh` on the exact tree.
- **GPT-5.5 plan review (pre-run):** folded in — per-capture `batchID` for #29 progress (a global count lies under interleave), scope #32's dismissal to state (not a bool flag), `isFallback` for #30, drop the #30 Settings deep-link, Layout A for #31, `changeCount` dedupe for #33, idempotent #56. Rebutted: GPT's "#56-first" reorder (no new app-target source files in the plan → no unblock benefit; kept Mit's committed order).
- **Epic #10 checkpoint:** 3 findings. (1) **MAJOR `batchCounts` leak on swipe of the last card** (#32 × #29) → FIXED in PR #82 (`pruneStaleBatchCounts()` + 2 tests). (2) MAJOR "failed-accept races swipe → order nondeterminism" → verified NO data loss (A and C both survive, only order differs; rare race, inherent to the accept-advance-before-persist design) → documented, not fixed. (3) MINOR Type-mode "Draft cards" label optimistic when AI unavailable → the `isFallback` note is honest → follow-up, not fixed.
- **Tooling checkpoint:** "No blocker found." Fixed the one MAJOR (CI now guards the `project.yml → Info.plist` key contract, PR #84). Rebutted/deferred: coarse patch idempotence (partial states only via manual pbxproj edits, caught by `count == 12`); hardcoded UUIDs / cross-machine determinism (pre-existing; committed pbxproj is CI-protected); `count == 12` (GPT agreed it's correctly fail-closed). Noted: `ARCHITECTURE.md:643` still describes the old 2-target/6-entry privacy ritual (docs follow-up).

## 5. Exact commands to review + merge the integration branch
```bash
cd /Users/mitsheth/Documents/anhkhooey
git fetch origin
git checkout overnight/backlog-2026-07 && git pull
git log --oneline --merges main..overnight/backlog-2026-07     # 23 PRs (session 1: #65–#75, session 2: #76–#84)
git diff --stat main..overnight/backlog-2026-07
bash scripts/ci.sh                                             # expect ✓ All checks passed
# When satisfied (and after ratifying the §2 decisions here + §2 of 2026-07-08.md):
#   open a PR from overnight/backlog-2026-07 → main, or fast-forward merge.
#   Issues #18–#34, #55, #56 auto-close when this reaches main (their PRs say "Fixes #NN").
```
Note: the repo lives under iCloud `~/Documents`; `make generate` this session spawned untracked `App/Anghkooey 2.xcodeproj` / ` 3` / ` 4` conflict dirs — they were removed and never staged (targeted `git add` throughout). Re-check with `find App Packages -name "* 2.*" -not -path "*/.build/*"` before any build.

## 6. What's LEFT
- **Ratify/reverse the ⚖️ decisions** in §2 (this session) and in `2026-07-08.md` §2 (session 1's #28).
- **Merge `overnight/backlog-2026-07` → `main`** (23 PRs, 48 commits ahead).
- **Follow-ups #85, #86** (small logging fixes) — filed, not fixed.
- **Stretch tiers — NOT started:** T1 #39/#40/#41 (library search/retry/tags), #53/#54 (a11y); T2 #48 (freeze), #35+#36 (widget advance+reveal), #38 (card deletion), #50 (sync restart UX). Each T2 is decision-bearing → needs a GPT-5.5 memo + extra review.
- **Out of scope (unchanged):** device-verification (F1), App Store submission (F5), the product-name check.
- **Docs nit:** update `ARCHITECTURE.md:643` (stale 2-target PrivacyInfo ritual) to match #56.
