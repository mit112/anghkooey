# Overnight backlog run — 2026-07-10 (session 3: main promotion + stretch tiers)

## 1. One-line verdict
**The 48-commit backlog (epics #8/#9/#10 + tooling #55/#56), all of Stretch Tier 1 (#39/#40/#41/#53/#54 + #85/#86), and Tier 2's #48 (freeze) are now MERGED TO `main`**, each gated on a full green `ci.sh` and a GPT-5.5 checkpoint; the remaining T2 units (#35+#36 widget, #38 card deletion, #50 sync-restart UX) were not started — resume plans in §6. **Environment note: iCloud actively corrupted the `~/Documents` working copy this session, so all work ran from a non-iCloud clone at `/private/tmp/anhkhooey-work` (Mit's copy is untouched; `git pull` it).**

## 2. ⚖️ Arbitrated decisions (GPT-5.5) — ratify or reverse
All reversible in code; **no on-disk schema/data-contract changes** this session.

- **#48 (NEW this session) — freeze = Option B "shift-on-return grace", NOT suppress-the-queue.** GPT-5.5-arbitrated (full memo on issue #48). The Settings copy claimed "no cards become due" while frozen, but the code never suppressed the review queue (freeze only records a timestamp; the shift happens on unfreeze). Rather than gate the review queue + clear the widget snapshot (Option A — rejected because clearing the widget snapshot overlaps the #35/#36 widget work reserved for this same session), we made the **copy honest**: reviewing stays available; on return the deck slides forward by whole away-days. Also surfaced unfreeze failures via `rootErrorPresenter` and documented the `floor(elapsed/86400)` rounding. **Now IN `main`** (PR #95). Meets the issue's acceptance criteria (copy-rewrite path). Reversible (copy string + doc + error handling).
- **Carried from sessions 1–2 (already in `main` since PR #87, still awaiting your ratification):** #28 dropped-capture = drop-with-message (defer transient-OCR retry); #31 Capture segment layout `Type|Camera|Cloze` default Type; #30 dropped the Settings deep-link (banner text suffices). See `2026-07-08.md` §2 and `2026-07-09.md` §2 for the full memos.

T1 (#39/#40/#41/#53/#54) carried **no** new arbitrated decisions (standard review gate). One notable **reversible product-behavior change** landed with #54: **tags now preserve the user's display casing** (case-insensitive uniqueness, first-writer casing wins) instead of being force-lowercased — matches the `Tag` model's documented mixed-case contract; flagged here for awareness.

## 3. Issues completed / merged, per tier

### FIRST ACTION — backlog → `main` ✅ (PR #87)
23 prior PRs / 48 commits (epics #8 review-loop, #9 stop-swallowing-errors, #10 capture/AI, tooling #55/#56). Auto-closed #18–#34, #55, #56.

### Follow-ups ✅ (PR #88 → integration; on `main` via T1 promotion)
| Issue | Notes |
|---|---|
| #85 | `OptimizedParametersStore.loadOptimized` — file-absent stays silent `nil`; present-but-corrupt logs via `CoreLog.scheduling` (throwing `readValidatedParameters()` + 6 tests). |
| #86 | `GradeCardIntent` logs the dropped grade before `.result()`. |

### Stretch Tier 1 ✅ (PRs #89–#92 → integration; promoted to `main` via PR #93)
| Issue | PR | Notes |
|---|---|---|
| #39 Library search | #89 | `.searchable` (Q/A/tag), composed AND with tag chip via testable pure `LibraryView.filter`; `ContentUnavailableView.search`; stale-tag-clear after reload. Subagent also fixed a real Swift-6 bug: `static filter` inherited `@MainActor` from `View` → `nonisolated`. |
| #40 Library retry | #89 | dead pull-to-refresh (non-scrollable) → visible **Retry** button. |
| #54 chip/tag a11y | #89 | `.isSelected` traits, hints, real 44pt hit targets (explicit frame, not `contentShape`), `Remove tag <name>` labels, tag-casing preserved. |
| #41 orphan-tag prune | #90 | `CardStore.update` prunes only the edited card's removed-and-orphaned tags (scoped, not a global sweep), in the save transaction. Reviewed (swift-reviewer + silent-failure-hunter). |
| #53 swipe cues | #91 | SF Symbol+label per direction, committed-state haptic, honest pre-reveal affordances, Reduce Motion; single-sourced eligibility with the commit path. |
| **T1 checkpoint hardening** | #92 | GPT-5.5 checkpoint found `submitEdit` echoing pre-canonicalization tag casing → `CardStore.update` now returns the persisted snapshot; `submitEdit` uses it. |

### Stretch Tier 2 — partial
| Issue | PR | Status |
|---|---|---|
| #48 freeze | #94 → **#95 to `main`** | ✅ done (⚖️ Option B). |
| #35 + #36 widget advance + reveal | — | **NOT STARTED** (§6). May need on-device verification. |
| #38 card deletion | — | **NOT STARTED** (§6). Cascade pre-arbitrated. |
| #50 sync restart UX | — | **NOT STARTED** (§6). |

### Follow-ups FILED this session (not fixed)
- **#96** — pre-existing double-unfreeze reentrancy (found by #48 checkpoint) + a minor freeze-copy overpromise. Low-harm (extra grace, not data loss); out of #48's scope.

## 4. Suite status & checkpoint findings
- **Before (session-3 start):** full `ci.sh` green — Core 150 / Intel 43 / UI 63 / app 165 (after cleaning an iCloud-corrupt `.build` + removing two `brctl`-materialized conflict-copy dups; see §5 and the iCloud memo).
- **After (main tip 92c35c9):** full `ci.sh` green — Core **162** / Intel **43** / UI **86** / app-target **167**.
- **GPT-5.5 T1 plan review (pre-tier):** 10 accepted critiques folded in — #41 scoped-prune (not a global sweep), avoid `#Predicate{cards.isEmpty}`, #53 not-leaf + reuse commit eligibility + hide grade cues pre-reveal, #54 real 44pt frames (not `contentShape`) + preserve tag casing, #39 clear stale selectedTag + derive-filter-once. Rebutted none of substance.
- **GPT-5.5 T1 cross-issue checkpoint:** caught the `submitEdit` tag-casing echo (fixed #92). Confirmed no Library duplicate-chip/prune/filter defect; #53 & #85 isolated.
- **GPT-5.5 #48 arbitration + checkpoint:** Option B chosen; checkpoint clean on the #48 changes (deferred a pre-existing reentrancy + copy nit → #96).
- **One CI flake:** a single non-deterministic SIGSEGV in a parallel Core `swift test` run (SwiftData in-memory containers under Swift Testing parallelism); did NOT reproduce in 8+ runs incl. the green gate. Logged; re-run resolves it. (Added to the iCloud/build memo.)

## 5. `main` state + commands Mit needs
- `main == overnight/backlog-2026-07 == 92c35c9`. Everything below is on `main`. Integration branch is NOT ballooning (0 ahead).
- **The working copy at `~/Documents/anhkhooey` is iCloud-corrupted from this session** (evicted top-level files, a corrupt `.build/build.db`, `brctl`-pulled conflict-copy dups). To resync your local copy:
  ```bash
  cd /Users/mitsheth/Documents/anhkhooey
  # sweep iCloud dup/conflict files (untracked):
  find App Packages \( -name .build -o -name .git \) -prune -o \( -name "* 2.*" -o -name "* 3.*" \) -print
  git fetch origin && git checkout main && git reset --hard origin/main   # discards the iCloud damage; all real work is on origin
  rm -rf Packages/*/.build .ci-derived-data
  bash scripts/ci.sh
  ```
- **Strong recommendation (root cause):** move the repo OUT of iCloud `~/Documents` (e.g. `~/Developer/anghkooey`). iCloud eviction/sync actively corrupted builds and git this session; the dup-sweep is only a mitigation. The clone at `/private/tmp/anhkhooey-work` was stable all session precisely because it's outside iCloud.

## 6. What's LEFT (Stretch Tier 2 — resume plans)
Each is decision-bearing: memo → GPT-5.5 arbitration → subagent (TDD) → review → per-issue checkpoint → merge. Start from `main`/integration @ 92c35c9 on a non-iCloud clone.
- **#35 + #36 (widget advance + reveal) — ONE unit; the reversibility hotspot.** Shared widget snapshot schema — version any on-disk shape or keep changes additive. **Likely needs on-device verification** (interactive widget/Live Activity behavior); the loop verifies on the simulator, so flag anything that truly needs hardware for Mit. Read the widget snapshot writer + `WidgetGradeReconciler` first.
- **#38 (card deletion).** The `ReviewLog` cascade was already arbitrated (confirm, don't re-open). Central persistence surface (adds a delete path to `CardStore` + UI + cascade) → full swift-reviewer + silent-failure-hunter. Note: #41's orphan-tag prune currently hooks only `update()`; wire the same prune into the new `delete()` path.
- **#50 (sync restart UX).** The iCloud-sync toggle needs an app restart to take effect (Settings copy already says so). Likely a small nudge/confirmation-UX fix; read `SyncPreference` + the Settings sync section.
- **#96 (filed):** double-unfreeze reentrancy guard + freeze-copy softening.
- **Out of scope (unchanged):** device-verification (F1), App Store submission (F5), product-name check.

## 7. Process notes (see the workflow-retro delta `2026-07-10-workflow-retro.md`)
The content-addressed CI gate (tree-hash proof) let several merges skip a redundant full `ci.sh` (integration-tip trees matched the green-gated PR trees). Per-issue subagent reports stayed on the fixed schema (CI log → file path). The single biggest cost/risk this session was the iCloud environment (see §5) and one un-resettable long Opus orchestrator context — the mechanical per-tier reset is only realizable if the harness restarts the context; here the ledger (`scratchpad/EXECUTION-LEDGER.md`) carried recovery state instead.
