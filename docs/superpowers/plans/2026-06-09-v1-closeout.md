# V1 Closeout Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans (inline) to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. Per project memory, detailed paste-ready plans run **inline on Sonnet** — do not dispatch subagents.

**Goal:** Take Anghkooey from "code-complete" to "shippable": fix every code-level finding from `docs/2026-06-09-v1-closeout-assessment.md`, prepare a maximally efficient one-shot device-QA script, then (later phase) icon/identity and submission mechanics.

**Architecture:** No new features — feature freeze as of M9. Phase 1 (Tasks 1–6) is code fixes + preparation, executable now with no device. Phase 2 (Tasks 7–9) is icon/identity, README, and submission, executed after Phase 1. The single Apple-Intelligence device session runs from the script produced in Task 6.

**Tech Stack:** SwiftUI, SwiftData, FoundationModels, xcodegen, xcodebuild, XcodeBuildMCP.

**Model routing (required per project policy):**

| Task | Model | Codex role | Reason |
|------|-------|------------|--------|
| 1–5 | Sonnet 4.6 | none | Small paste-ready diffs; subagents/Codex add cost, no review benefit |
| 6 | Sonnet 4.6 | none | Checklist compilation from existing docs |
| 7 | Sonnet 4.6 | none | Asset catalog + xcodegen ritual; mechanical |
| 8 | **Opus 4.7** | none | README is a recruiter-facing portfolio write-up (CLAUDE.md routing rule) |
| 9 | Sonnet 4.6 | none | Executes existing M6 plan tasks; mechanical |
| Device session | User (manual) | — | Requires Apple Intelligence iPhone |

**Verification commands used throughout** (sim ID drifts — re-verify with `xcodebuild -showdestinations` at session start; as of 2026-06-09 iPhone 17 Pro = `89D97E0D-E6DF-4899-91DE-38E1B1EAF446`):

```bash
# BUILD
xcodebuild build -project /Users/mitsheth/Documents/rewind/App/Anghkooey.xcodeproj \
  -scheme Anghkooey -destination "id=<SIM_ID>" \
  -derivedDataPath /tmp/anghkooey-derived-data \
  CODE_SIGN_IDENTITY="-" CODE_SIGNING_REQUIRED=NO CODE_SIGN_STYLE=Manual 2>&1 | grep -E "error:|warning:|Build complete"

# TEST (full app suite, ~90 s; expect 107+ tests green)
xcodebuild test -project /Users/mitsheth/Documents/rewind/App/Anghkooey.xcodeproj \
  -scheme Anghkooey -destination "id=<SIM_ID>" \
  -derivedDataPath /tmp/anghkooey-derived-data \
  CODE_SIGN_IDENTITY="-" CODE_SIGNING_REQUIRED=NO CODE_SIGN_STYLE=Manual 2>&1 | tail -3
```

---

## Phase 1 — Code readiness (do now)

### Task 0: Commit untracked plan docs

Six plan docs are untracked (`git status`): m5.5m, m5.5s, m5.5t, m6, v1.1, m8 plans.

- [ ] **Step 0.1:**
```bash
cd /Users/mitsheth/Documents/rewind
git add docs/superpowers/plans/ docs/2026-06-09-v1-closeout-assessment.md
git commit -m "docs: track outstanding plan docs + v1 closeout assessment"
```

---

### Task 1: F3a — Onboarding page dots visible + Skip affordance

**Files:**
- Modify: `Packages/AnghkooeyUI/Sources/AnghkooeyUI/Onboarding/OnboardingView.swift:43-46`

- [ ] **Step 1.1: Apply the fix.** Replace:

```swift
        .tabViewStyle(.page)
        .ignoresSafeArea()
```

with:

```swift
        .tabViewStyle(.page)
        .indexViewStyle(.page(backgroundDisplayMode: .always))
        .ignoresSafeArea()
        .overlay(alignment: .topTrailing) {
            Button("Skip") { onFinish() }
                .padding(.top, 64)
                .padding(.trailing, 24)
                .accessibilityLabel("Skip onboarding")
        }
```

- [ ] **Step 1.2: Build** (BUILD command above). Expected: `Build complete!`, no new warnings.

- [ ] **Step 1.3: Visual check.** Reset the flag and relaunch; screenshot must show grey page-dot capsule at bottom and Skip at top right:
```bash
xcrun simctl spawn <SIM_ID> defaults delete com.mitsheth.anghkooey hasCompletedOnboarding
xcrun simctl terminate <SIM_ID> com.mitsheth.anghkooey; xcrun simctl launch <SIM_ID> com.mitsheth.anghkooey
```
Then `mcp__XcodeBuildMCP__screenshot`.

- [ ] **Step 1.4: Commit**
```bash
git add Packages/AnghkooeyUI/Sources/AnghkooeyUI/Onboarding/OnboardingView.swift
git commit -m "fix(ui): visible onboarding page dots + Skip button"
```

---

### Task 2: F3b — Surface sample-deck load failures in ContentView

**Files:**
- Modify: `App/Anghkooey/ContentView.swift` (removes the three `try?` swallows at lines 27, 73, 91)

- [ ] **Step 2.1: Replace the `sampleLoader()` helper** (lines 17–19) with:

```swift
    @State private var sampleLoadErrorMessage: String?

    private func loadSamples() async {
        do {
            try await SampleDeckLoader(store: appState.cardStore).load(now: .now)
        } catch {
            sampleLoadErrorMessage = "Couldn't load the sample deck: \(error.localizedDescription)"
        }
    }
```

- [ ] **Step 2.2: Replace all three call sites:**
  - Line 27: `loadSampleCards: { try? await sampleLoader().load(now: .now) }` → `loadSampleCards: { await loadSamples() }`
  - Line 73: same replacement inside `LibraryView(...)`.
  - Lines 88–93 (onboarding `onLoadSample`): replace the whole closure body with `Task { await loadSamples() }` (delete the local `loader` lines).

- [ ] **Step 2.3: Add the alert** after `.safeAreaInset(edge: .top) { ... }` (end of `body`):

```swift
        .alert(
            "Sample deck",
            isPresented: Binding(
                get: { sampleLoadErrorMessage != nil },
                set: { if !$0 { sampleLoadErrorMessage = nil } }
            )
        ) {
            Button("OK", role: .cancel) {}
        } message: {
            Text(sampleLoadErrorMessage ?? "")
        }
```

- [ ] **Step 2.4: Build + full TEST.** Expected: green; the three "result of 'try?' is unused" warnings are gone.

- [ ] **Step 2.5: Commit**
```bash
git add App/Anghkooey/ContentView.swift
git commit -m "fix(app): surface sample-deck load failures instead of swallowing (F3b)"
```

---

### Task 3: F3c — Warning cleanup (with explicit DO-NOT-FIX list)

**DO NOT "fix" these warnings — they are false positives:** the five `'public' modifier is redundant` warnings in `AnghkooeySchemaV2–V5.swift` / `Card.swift`. Per project memory, removing `public` from `@Model` classes inside `public extension` **breaks PersistentModel conformance**. Leave them.

**Files:**
- Modify: `Packages/AnghkooeyCore/Sources/AnghkooeyCore/Widget/WidgetBridge.swift:36,39`
- Modify: `App/AnghkooeyWidget/AnghkooeyWidgetBundle.swift:89,99`
- Modify: `Packages/AnghkooeyCore/Sources/AnghkooeyCore/Import/AnkiPackageParser.swift:63`
- Modify: `App/Anghkooey/Info.plist`

- [ ] **Step 3.1:** `WidgetBridge.swift:36,39` — delete the `nonisolated(unsafe)` prefix on the `JSONEncoder` and `JSONDecoder` constants (both types are Sendable now).

- [ ] **Step 3.2:** `AnghkooeyWidgetBundle.swift:89` — change `var intent` to `let intent`. Line 99 — delete the `nonisolated(unsafe)` prefix on the `String` constant.

- [ ] **Step 3.3:** `AnkiPackageParser.swift:63` — replace the deprecated optional initializer with the throwing one. Current shape is `Archive(url:accessMode:preferredEncoding:)` used optionally; change to `try Archive(url: <same url>, accessMode: .read)` and delete the `preferredEncoding:` argument. If the surrounding code `guard let`s the optional, replace with a direct `try` (the enclosing function already throws — verify; if not, wrap in `do/catch` mapping to the parser's existing error type at that call site).

- [ ] **Step 3.4:** `App/Anghkooey/Info.plist` — add inside the top-level `<dict>`:
```xml
	<key>LSSupportsOpeningDocumentsInPlace</key>
	<false/>
```
(We import `.apkg` files by copying; we never edit in place.)

- [ ] **Step 3.5: Build + full TEST.** Expected: green; warning count drops from 14 to ≤ 6 (5 false-positive `public` + possibly the SwiftData "Unbinding from main queue" runtime notice, which is not a compile warning).

- [ ] **Step 3.6: Commit**
```bash
git add Packages/AnghkooeyCore/Sources/AnghkooeyCore/Widget/WidgetBridge.swift \
        App/AnghkooeyWidget/AnghkooeyWidgetBundle.swift \
        Packages/AnghkooeyCore/Sources/AnghkooeyCore/Import/AnkiPackageParser.swift \
        App/Anghkooey/Info.plist
git commit -m "chore: clear compiler warnings (keep false-positive @Model publics)"
```

---

### Task 4: F4 — Privacy manifest CA92.1 + correct the README audit

UserDefaults is used by **app-target code only** (verified 2026-06-09: `AnghkooeyApp.swift`, `ClipboardCaptureCoordinator.swift`, `FreezeController.swift`, `SyncPreference.swift`, plus `OnboardingView.swift` in AnghkooeyUI which links into the app). Share extension and Widget do **not** use it — their manifests stay unchanged.

**Files:**
- Modify: `App/Anghkooey/PrivacyInfo.xcprivacy`
- Modify: `README.md`

- [ ] **Step 4.1:** In `App/Anghkooey/PrivacyInfo.xcprivacy`, inside the `NSPrivacyAccessedAPITypes` array, add after the FileTimestamp dict:

```xml
        <dict>
            <key>NSPrivacyAccessedAPIType</key>
            <string>NSPrivacyAccessedAPICategoryUserDefaults</string>
            <key>NSPrivacyAccessedAPITypeReasons</key>
            <array>
                <string>CA92.1</string>
            </array>
        </dict>
```

- [ ] **Step 4.2:** In `README.md`:
  - Delete the row `| NSPrivacyAccessedAPICategoryUserDefaults | Not used — no UserDefaults or @AppStorage in production code |` from the "not used" table.
  - Add to the declared-APIs table:
```markdown
| `NSPrivacyAccessedAPICategoryUserDefaults` | `CA92.1` | Main app (`OnboardingState`, `SyncPreference`, `FreezeController`, `ClipboardCaptureCoordinator`) | App's own settings only: onboarding-completed flag, iCloud sync opt-in, freeze state, clipboard-offer dedupe. No third-party access. |
```

- [ ] **Step 4.3:** Verify the manifest is still valid plist + still in the build phase:
```bash
plutil -lint App/Anghkooey/PrivacyInfo.xcprivacy
grep -c "PrivacyInfo" App/Anghkooey.xcodeproj/project.pbxproj   # expected ≥ 6
```

- [ ] **Step 4.4: Build + Commit**
```bash
git add App/Anghkooey/PrivacyInfo.xcprivacy README.md
git commit -m "fix(privacy): declare UserDefaults CA92.1; correct README required-reason audit"
```

---

### Task 5: F1-prep — Expand eval fixtures 3 → 20

`RubricScorer` is a pure string scorer (atomicity/specificity/groundedness/leakage vs the passage) — new fixtures need only a `passage`. `goldenDrafts: []` is valid: `inputPasses` on an empty array is vacuously true, so CI stays green; goldens get populated on the AI machine during the device session via `EvalRunner --update-goldens`.

**Files:**
- Modify: `Packages/AnghkooeyIntelligence/Tests/AnghkooeyIntelligenceTests/Fixtures/eval-fixtures.json`

- [ ] **Step 5.1:** Append these 17 objects to the JSON array (existing ids: `biology-001`, `vocab-001`, `history-001` — keep them):

```json
{"id":"chemistry-001","passage":"A covalent bond forms when two atoms share one or more pairs of electrons. Water is held together by polar covalent bonds because oxygen attracts the shared electrons more strongly than hydrogen does.","templateVersion":"v1.0","goldenDrafts":[]},
{"id":"physics-001","passage":"Newton's second law states that the net force on an object equals its mass times its acceleration. Doubling the force on a fixed mass doubles the acceleration.","templateVersion":"v1.0","goldenDrafts":[]},
{"id":"history-002","passage":"The Treaty of Westphalia in 1648 ended the Thirty Years' War. It established the principle of state sovereignty, under which each ruler determined the affairs of their own territory.","templateVersion":"v1.0","goldenDrafts":[]},
{"id":"geography-001","passage":"The Mariana Trench in the western Pacific Ocean is the deepest part of the world's oceans, reaching about 10,994 meters at the Challenger Deep.","templateVersion":"v1.0","goldenDrafts":[]},
{"id":"vocab-002","passage":"Ephemeral means lasting for a very short time. A mayfly's adult life is ephemeral, often lasting only a single day.","templateVersion":"v1.0","goldenDrafts":[]},
{"id":"cs-001","passage":"A hash table stores key-value pairs and offers average constant-time lookup. Collisions occur when two keys map to the same bucket and are commonly resolved by chaining or open addressing.","templateVersion":"v1.0","goldenDrafts":[]},
{"id":"medicine-001","passage":"Insulin is a hormone produced by the beta cells of the pancreas. It lowers blood glucose by signaling cells in the liver and muscles to absorb glucose from the bloodstream.","templateVersion":"v1.0","goldenDrafts":[]},
{"id":"astronomy-001","passage":"A light-year is the distance light travels in one year, about 9.46 trillion kilometers. Proxima Centauri, the nearest star to the Sun, is roughly 4.24 light-years away.","templateVersion":"v1.0","goldenDrafts":[]},
{"id":"economics-001","passage":"Opportunity cost is the value of the best alternative forgone when a choice is made. Spending an evening studying carries the opportunity cost of the wages from a missed work shift.","templateVersion":"v1.0","goldenDrafts":[]},
{"id":"art-001","passage":"Chiaroscuro is the use of strong contrast between light and dark in painting. Caravaggio used chiaroscuro to give his figures dramatic three-dimensional volume.","templateVersion":"v1.0","goldenDrafts":[]},
{"id":"music-001","passage":"In music, a fugue is a contrapuntal composition in which a short melody, the subject, is introduced by one voice and successively taken up by the others. Bach's The Well-Tempered Clavier contains forty-eight preludes and fugues.","templateVersion":"v1.0","goldenDrafts":[]},
{"id":"psychology-001","passage":"The spacing effect is the finding that learning is more durable when study sessions are spread out over time rather than massed together. Reviewing material at increasing intervals improves long-term retention.","templateVersion":"v1.0","goldenDrafts":[]},
{"id":"math-001","passage":"A prime number is a natural number greater than one whose only divisors are one and itself. The number two is the only even prime.","templateVersion":"v1.0","goldenDrafts":[]},
{"id":"biology-002","passage":"Photosynthesis converts carbon dioxide and water into glucose and oxygen using light energy. It takes place in the chloroplasts of plant cells, primarily within the leaves.","templateVersion":"v1.0","goldenDrafts":[]},
{"id":"literature-001","passage":"An unreliable narrator is a storyteller whose account the reader has reason to doubt. The narrator of Edgar Allan Poe's The Tell-Tale Heart insists on his sanity while describing his own madness.","templateVersion":"v1.0","goldenDrafts":[]},
{"id":"anatomy-001","passage":"The human heart has four chambers: two atria that receive blood and two ventricles that pump it out. The left ventricle has the thickest wall because it pumps blood to the entire body.","templateVersion":"v1.0","goldenDrafts":[]},
{"id":"language-001","passage":"In Japanese, the particle wa marks the topic of a sentence, while ga marks the grammatical subject. The sentence watashi wa gakusei desu means I am a student.","templateVersion":"v1.0","goldenDrafts":[]}
```

- [ ] **Step 5.2: Validate:**
```bash
python3 -c "
import json
d = json.load(open('Packages/AnghkooeyIntelligence/Tests/AnghkooeyIntelligenceTests/Fixtures/eval-fixtures.json'))
ids = [f['id'] for f in d]
assert len(d) == 20, len(d)
assert len(set(ids)) == 20, 'duplicate ids'
assert all(set(f) == {'id','passage','templateVersion','goldenDrafts'} for f in d)
print('OK — 20 fixtures')"
```

- [ ] **Step 5.3:** Note in `docs/EVALS/m5-eval-run.md` under the pending section: `Fixture set expanded 3 → 20 on 2026-06-09; goldens for the 17 new fixtures pending first --update-goldens run on AI hardware.`

- [ ] **Step 5.4: Run full TEST** (AnghkooeyIntelligence tests don't run via `swift test` on macOS — fatalError by design; the app-scheme run is the check). Expected: green.

- [ ] **Step 5.5: Commit**
```bash
git add Packages/AnghkooeyIntelligence/Tests/AnghkooeyIntelligenceTests/Fixtures/eval-fixtures.json docs/EVALS/m5-eval-run.md
git commit -m "eval: expand fixture set 3 -> 20 passages; goldens pending AI-device run"
```

---

### Task 6: Device-QA session script (the one-shot batched checklist)

**Files:**
- Create: `docs/EVALS/device-qa-session.md`

- [ ] **Step 6.1:** Create the file with exactly this content:

```markdown
# One-Shot Device QA Session — Apple Intelligence iPhone

> Prereq: iPhone 15 Pro or later, iOS 26+, Apple Intelligence enabled and model downloaded
> (Settings → Apple Intelligence & Siri). Budget: ~2.5 hours including the 30-min soak.
> Bring: this checklist, a Mac with Xcode + this repo, a Lightning/USB-C cable.
> Order matters — items are sequenced so reboots/installs don't invalidate earlier steps.

## 0. Setup (10 min)
- [ ] Connect device, trust Mac, enable Developer Mode (Settings → Privacy & Security).
- [ ] Xcode: select device, automatic signing, ⌘R install. App launches → onboarding appears.

## 1. Eval harness + goldens (20 min) — foundation.md §9.2
- [ ] On the Mac (if it has Apple Intelligence) or via the device test host:
      `swift run --package-path Packages/AnghkooeyIntelligence EvalRunner --update-goldens`
      (populates goldens for the 17 new fixtures)
- [ ] `swift run --package-path Packages/AnghkooeyIntelligence EvalRunner` → record pass rate.
- [ ] Gate: ≥ 80% (≥ 16/20). Record per-fixture results in docs/EVALS/m5-eval-run.md.
- [ ] If < 80%: note failing fixtures + failure mode; prompt iteration happens AFTER the
      session (don't burn device time on prompt engineering).

## 2. M9 deferred AI items (15 min)
- [ ] Q&A capture E2E: Safari → select text → Share → Anghkooey → AI draft sheet shows real
      Q/A (not the fallback stub) → edit → accept → card appears in Review tab.
- [ ] Cloze E2E: Capture tab → Cloze mode → paste a sentence → AI proposes cloze deletions →
      accept → cloze card reviews correctly (blank shown, answer on reveal).
- [ ] Time the share-to-draft latency with a stopwatch, 3 samples. Gate: < 5 s median
      (foundation §9.1). Record numbers.

## 3. E1 — Airplane mode fallback (10 min)
- [ ] Airplane mode ON → share text from Safari → fallback draft (question = shared text,
      answer = "(edit to add answer)") → accept → appears in Review.
- [ ] Airplane mode OFF → share again → real AI draft. Check E1 boxes in
      docs/EVALS/resilience-checklist.md.

## 4. Instruments traces (25 min) — PERFORMANCE.md artifacts
- [ ] ⌘I (Profile) → Blank template + os_signpost instrument.
- [ ] Record: seed 3 cards by typing, review 10 cards. Stop.
- [ ] Screenshot `review-tap` intervals → docs/instruments/m6-review-tap-signpost.png
- [ ] Same trace shows `ai-draft-generation` interval from a share capture →
      screenshot → docs/instruments/ai-draft-generation.png
- [ ] Settings → Optimize Schedule → run optimizer → screenshot `fsrs-optimization`
      POI trace → docs/instruments/fsrs-optimization.png

## 5. E2 — 30-minute soak (30 min, can overlap coffee)
- [ ] 20+ cards in queue, review continuously with varied pacing; idle 2–3 min stretches.
- [ ] Xcode Memory Report attached: no unbounded growth. No hangs/ANR.
- [ ] Check E2 boxes in resilience-checklist.md.

## 6. Wrap-up (10 min)
- [ ] E3 (lower-tier device): if iPhone 15 still available, fallback path is already verified
      (2026-06-03 session) — mark E3 with that date/device; otherwise note "verified on
      iPhone 15, 2026-06-03" in App Review notes.
- [ ] Commit: updated m5-eval-run.md, resilience-checklist.md, goldens JSON, 3 PNGs.
- [ ] ~24h later (device can be returned): MetricKit payload appears in Console.app
      (subsystem com.mitsheth.anghkooey, category MetricKit) only after a TestFlight/dev
      run — if captured, paste into PERFORMANCE.md §M6.
```

- [ ] **Step 6.2: Commit**
```bash
git add docs/EVALS/device-qa-session.md
git commit -m "docs: one-shot Apple Intelligence device QA script"
```

---

## Phase 2 — Identity + submission (after Phase 1; icon deliberately deferred per 2026-06-09 decision)

### Task 7: App icon + accent color (asset catalog)

**Files:**
- Create: `App/Anghkooey/Assets.xcassets/Contents.json`
- Create: `App/Anghkooey/Assets.xcassets/AppIcon.appiconset/Contents.json` (+ `AppIcon.png`)
- Create: `App/Anghkooey/Assets.xcassets/AccentColor.colorset/Contents.json`
- Modify: `App/project.yml` (two settings lines)

- [ ] **Step 7.1: Create catalog scaffolding:**

`App/Anghkooey/Assets.xcassets/Contents.json`:
```json
{ "info": { "author": "xcode", "version": 1 } }
```

`App/Anghkooey/Assets.xcassets/AppIcon.appiconset/Contents.json` (single-size, iOS):
```json
{
  "images": [
    { "filename": "AppIcon.png", "idiom": "universal", "platform": "ios", "size": "1024x1024" }
  ],
  "info": { "author": "xcode", "version": 1 }
}
```

`App/Anghkooey/Assets.xcassets/AccentColor.colorset/Contents.json` (brand violet `#6246EA` — distinct from default system blue; same in dark mode):
```json
{
  "colors": [
    {
      "color": {
        "color-space": "srgb",
        "components": { "alpha": "1.000", "blue": "0xEA", "green": "0x46", "red": "0x62" }
      },
      "idiom": "universal"
    }
  ],
  "info": { "author": "xcode", "version": 1 }
}
```

- [ ] **Step 7.2: Icon artwork (user-interactive).** Generate candidates (ecc:ios-icon-gen skill or fal.ai image gen): concept direction — memory/recall motif (e.g., stylized card stack with a spark), flat, bold single shape, violet `#6246EA` background, no text. Export **1024×1024 PNG, no alpha channel** → `App/Anghkooey/Assets.xcassets/AppIcon.appiconset/AppIcon.png`. Verify: `sips -g hasAlpha AppIcon.png` → `hasAlpha: no`.

- [ ] **Step 7.3:** In `App/project.yml`, add to the `Anghkooey` target's `settings.base` (after `CURRENT_PROJECT_VERSION`):
```yaml
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
```

- [ ] **Step 7.4: Regenerate + recovery ritual** (xcodegen is destructive — all four steps required, per project memory):
```bash
make generate
python3 scripts/patch_privacy_info.py        # re-adds .xcprivacy build-phase entries
grep -c "PrivacyInfo" App/Anghkooey.xcodeproj/project.pbxproj   # expect ≥ 6
grep -c "NSCameraUsageDescription" App/Anghkooey/Info.plist || echo "RE-ADD camera key"
```
If the camera key is missing, re-add to `App/Anghkooey/Info.plist`:
```xml
	<key>NSCameraUsageDescription</key>
	<string>Anghkooey uses the camera to scan books, whiteboards, and notes into flashcards.</string>
```
Also `git diff App/Anghkooey.xcodeproj` and revert any signing flips (`CODE_SIGN_IDENTITY` must stay `"-"`, style `Manual`), and re-verify the test target still has `GENERATE_INFOPLIST_FILE=YES` + scheme Testables entry.

- [ ] **Step 7.5: Build + full TEST + visual check.** Install on sim; home-screen icon must show the new artwork (not the blank grid). Buttons/tints across the app render violet.

- [ ] **Step 7.6: Commit**
```bash
git add App/Anghkooey/Assets.xcassets App/project.yml App/Anghkooey.xcodeproj App/Anghkooey/Info.plist
git commit -m "feat(identity): app icon + brand accent color (#6246EA)"
```

---

### Task 8: README portfolio rewrite — **switch to Opus (`/model opus`)**

**Files:**
- Modify: `README.md`
- Create: `docs/screenshots-readme/` (3 sim screenshots)

- [ ] **Step 8.1:** Capture 3 screenshots on sim (post-icon, post-accent): Review session card, Library with tag chips, Capture tab → `docs/screenshots-readme/{review,library,capture}.png`.

- [ ] **Step 8.2:** Rewrite README with this structure (prose authored by Opus at execution; every section below must exist):
  1. Title + tagline + 3-screenshot row (markdown image table).
  2. What it is — 3-sentence pitch (capture-first, on-device AI authoring, grace-first FSRS-6).
  3. Highlights for engineers — bullet list linking each to evidence: FSRS-6 port + 150-fixture parity harness, FoundationModels `@Generable` pipeline + rubric eval harness, SwiftData V1→V5 versioned migrations, SPM 3-module split, Swift 6 strict concurrency, App Intents/WidgetKit/CloudKit opt-in, MetricKit + signposts.
  4. Architecture — link `ARCHITECTURE.md`, one-paragraph module map (Core ← UI, Core ← Intelligence, app composes).
  5. Performance — link `PERFORMANCE.md`, quote the review-tap number.
  6. Privacy — the existing required-reason audit tables (keep, already corrected in Task 4).
  7. Build & test — exact xcodebuild commands from this plan's header.

- [ ] **Step 8.3: Commit**
```bash
git add README.md docs/screenshots-readme/
git commit -m "docs: portfolio-grade README (Opus)"
```

---

### Task 9: Submission mechanics — execute the existing M6 plan

The full step-by-step detail already exists in `docs/superpowers/plans/2026-05-22-m6-beta-submission.md`. Execute it with these corrections to its task list:

- [ ] **Step 9.1:** M6 Task 1 (metadata description rewrite + bundle ID + screenshot plan) — run as written, but add Cloze cards, Anki import, FSRS optimization, and the widget to the description (features shipped after that plan was written: M7/M8/v1.5/v1.1 lanes).
- [ ] **Step 9.2:** M6 Task 2 — **skip** (MARKETING_VERSION/CURRENT_PROJECT_VERSION already in `App/project.yml:91-92`, verified 2026-06-09).
- [ ] **Step 9.3:** M6 Task 3 (privacy policy hosting) — file already exists at `docs/STORE/PRIVACY_POLICY.md`; do the hosting (GitHub Pages) + metadata URL steps only. Update its "Last updated" date.
- [ ] **Step 9.4:** M6 Task 10 (name availability: App Store, USPTO TESS, .com/.app) — **must pass before any public push** (foundation §0). If "Anghkooey" is taken anywhere material, stop and decide with the user.
- [ ] **Step 9.5:** M6 Task 11 (6 App Store screenshots, 1320×2868) — run as written, after Task 7 so the icon/accent shows.
- [ ] **Step 9.6:** M6 Tasks 12–13 (final checklist, Archive, TestFlight) — run as written; Tasks 4–5 (latency test + PERFORMANCE.md numbers) — check whether `App/AnghkooeyTests/ReviewTapLatencyTests.swift` exists first; if it does, just transcribe its latest numbers.
- [ ] **Step 9.7:** After TestFlight upload: portfolio site update + LinkedIn post (user-authored; offer a draft).

---

## Exit criteria (project end — from the assessment's Definition of Done)

1. Tasks 0–6 complete, suite green, warnings ≤ 6 (the false positives).
2. Tasks 7–8 complete (icon, accent, README).
3. Device session executed per `docs/EVALS/device-qa-session.md`; eval ≥ 80% recorded.
4. Task 9 complete through App Store submission.
5. Portfolio + LinkedIn published. **Then stop — no further milestones.**
