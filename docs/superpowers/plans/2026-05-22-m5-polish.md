# M5 Polish, Perf, Privacy, Eval — Stub
**Date:** 2026-05-22
**Branch:** `m5/polish` (not yet created)
**Plan source:** `docs/superpowers/plans/2026-05-20-rewind-implementation-plan.md §M5`
**Status:** Stub. Full plan to be authored on `/model opus` at M5 entry.

---

## Entry gate (carry-overs from M4)

Before M5 starts, the following M4 device-verification items must be cleared:
- iPhone 15 device build — clean, no new warnings
- Demo path on device: share via Share Extension → review sheet → Accept → Review tab → Got it → empty
- Repeat with Missed it → card reappears immediately

If any of these fail, M5 absorbs the fix as M5.0.

---

## Exit gate (from §M5 of master plan)

1. `PERFORMANCE.md` published with:
   - Instruments `.trace` for a 30-min review session
   - MetricKit payload screenshot (`MXMetricPayload` capture wired)
   - `os_signpost` ranges around: drain → author → enqueue, review-tap → next-card, OCR text recognition
   - Before/after metrics on at least one optimization
2. `PrivacyInfo.xcprivacy` audited; required-reason API list documented in README
3. Airplane-mode E2E pass (capture → author → review entirely offline)
4. 30-min review session: no main-thread block >100ms, memory growth <20MB
5. Eval harness (M2) re-run; pass-rate documented in `docs/EVALS/`
6. App Store metadata draft (description, screenshots, support URL)
7. Manual QA checklist green on iPhone 15 Pro + one lower-tier device

---

## Provisional task lanes

These are placeholders; sequencing TBD at M5 entry review.

### Lane A — Schema completion (M4 follow-up)
- M5.A1 Extend `Card` model to persist `reps`, `lapses`, `learningSteps`, `scheduledDays`, `elapsedDays` (step-machine fields currently defaulted to 0 in `Card.Snapshot.schedulingCard`)
- M5.A2 SwiftData migration plan (`AnghkooeySchemaV2` + `MigrationPlan`) — first non-trivial migration
- M5.A3 Update `CardStore.create` / `apply` to read/write the new fields
- M5.A4 Parity-harness re-run against ts-fsrs with full step-machine state preserved

### Lane B — Performance instrumentation
- M5.B1 Add `OSSignposter` intervals: `drain.author`, `review.tap`, `ocr.recognize`
- M5.B2 Capture baseline Instruments trace on iPhone 15
- M5.B3 MetricKit subscriber: persist `MXMetricPayload` summaries to disk, render one screenshot for `PERFORMANCE.md`
- M5.B4 Profile one suspected hotspot and document before/after

### Lane C — Privacy + App Store readiness
- M5.C1 Audit `PrivacyInfo.xcprivacy` against actually-used APIs (file timestamps, system boot time, disk space, user defaults)
- M5.C2 Required-reason API table in `README.md`
- M5.C3 App Store metadata draft (`docs/STORE/`)
- M5.C4 Privacy manifest for app extensions (Share Extension) — currently xcprivacy count is 8 after `make generate`

### Lane D — Eval re-run + small UX polish
- M5.D1 Re-run M2 eval harness against the shipped `LiveCardAuthoringService` config; commit pass-rate to `docs/EVALS/`
- M5.D2 Haptics on Got it / Missed it (M4 deferred this)
- M5.D3 Review-screen empty-state polish (SF Symbol pick, copy iteration)
- M5.D4 Capture-tab integration: confirm in-app camera path still produces drafts post-M4

### Lane E — Offline + resilience
- M5.E1 Airplane-mode capture: confirm Foundation Models falls back gracefully (M4 already has a stub fallback)
- M5.E2 30-min soak test: log main-thread hangs via `MXHangDiagnostic`, memory growth via `MXMetaData`
- M5.E3 Crash-free path verification on a lower-tier device (SE / 15)

---

## Risk register (preliminary)

| Risk | Likelihood | Mitigation |
|------|-----------|-----------|
| SwiftData migration on shipped store is destructive | Medium | M5.A2 designs migration with a backup; M4 store is single-user dev data — acceptable to wipe in dev |
| MetricKit payload only delivered every 24h | High (known) | Use `MXMetricManager` subscriber from day 1; manual trigger via simulator is sufficient for `PERFORMANCE.md` |
| Lower-tier device perf gate fails | Medium | Cut-line per §M5: narrow App Store device matrix to iPhone 15+ |
| Eval harness regression after M4 changes to Mock / Live authoring | Low | M5.D1 first; if regressed, prompt-iteration ADR |

---

## Out-of-scope (still deferred)

- CloudKit sync wiring (transport; schema is ready)
- 4-button grading UI
- WidgetKit / Live Activity
- Tag UI / decks / statistics
- Cloze deletion
- Audio/image cards
- Card editing before accept

---

## Model routing for M5

Per `CLAUDE.md` model-routing policy + [[project-collaboration-workflow]]. Each lane's detailed plan MUST include a per-task model table (see `2026-05-22-m5a-schema-completion.md` for the canonical shape). Lane-level defaults:

| Lane | Default Model | Codex hand-off candidates | Opus only for |
|------|---------------|---------------------------|---------------|
| A — Schema | Sonnet | M5.A Task 5 (if migration not lightweight), Task 7 `apply` patch | Migration design ADR if `.lightweight` fails |
| B — Performance | Sonnet | Hot-path optimization with measurable before/after | `PERFORMANCE.md` write-up + profiling judgment |
| C — Privacy + Store | Sonnet | None (metadata, not code) | None |
| D — Eval + UX polish | Sonnet | None — too small to warrant a round-trip | Eval analysis if pass-rate regressed |
| E — Offline + soak | Sonnet | Soak harness if it grows past ~50 LOC | None |

Universal rule: never use Opus for execution. Escalate to Opus only after a task gets stuck twice on Sonnet, or for design/review work explicitly listed in CLAUDE.md.
